version: '3.8'

services:
  api:
    build: 
      context: .
      network: host    
    volumes:
        - $HOME/datasets/:/datasets/
    network_mode: "host" 
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 2            
              capabilities: [gpu]
